---
phase: 01-design-foundation-motion-system
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/tasks/criticality-picker.tsx
  - src/components/tasks/nav-tabs.tsx
  - src/components/tasks/view-tabs.tsx
autonomous: true
requirements: [FOUND-03, MOTION-02]
must_haves:
  truths:
    - "The criticality picker's three dots read as points along the teal→violet gradient (on_track=teal, warning=mid, critical=violet), not red/amber/green hex"
    - "Project criticality left-borders (projects page) use the same gradient-derived colors via the shared CRITICALITY_COLORS map"
    - "Nav tabs and view tabs show explicit token-driven hover/press feedback (duration-micro; view-tab press translate)"
  artifacts:
    - path: "src/components/tasks/criticality-picker.tsx"
      provides: "Token-driven criticality colors + accessible dots"
      contains: "var(--gradient-violet)"
    - path: "src/components/tasks/view-tabs.tsx"
      provides: "View-tab control with explicit motion + press feedback"
      contains: "pressFeedback"
  key_links:
    - from: "src/components/tasks/criticality-picker.tsx CRITICALITY_COLORS"
      to: "src/app/tasks/projects/page.tsx borderColor"
      via: "shared exported color map (values change, keys/consumer unchanged)"
      pattern: "CRITICALITY_COLORS\\[project.criticality\\]"
    - from: "src/components/tasks/nav-tabs.tsx / view-tabs.tsx"
      to: "src/lib/motion.ts"
      via: "import of transitionMicro / pressFeedback"
      pattern: "from \"@/lib/motion\""
---

<objective>
Make the two components explicitly named in the phase consume the new token layer: recolor the criticality picker onto the teal→violet functional gradient (FOUND-03), and give nav/view tabs explicit token-driven hover/press feedback (MOTION-02). Both are restyles — no prop, callback, or accessibility markup changes — satisfying the functional-parity constraint.

Purpose: Deliver the first visible functional use of the brand gradient and consistent interactive feedback on the shared navigation controls.
Output: Three restyled components consuming `globals.css` tokens and `src/lib/motion.ts` constants.
</objective>

<execution_context>
@/Users/erx/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/erx/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-design-foundation-motion-system/01-CONTEXT.md
@.planning/phases/01-design-foundation-motion-system/01-RESEARCH.md
@.planning/phases/01-design-foundation-motion-system/01-PATTERNS.md
@.planning/phases/01-design-foundation-motion-system/01-UI-SPEC.md

<interfaces>
<!-- From src/lib/motion.ts (created in Plan 01-01). Import and use directly. -->
export const transitionMicro = "transition-colors duration-micro ease-out"
export const transitionBase = "transition-all duration-base ease-out-soft"
export const pressFeedback = "active:translate-y-px"

<!-- Gradient tokens available in globals.css after Plan 01-01: -->
<!-- var(--gradient-teal) = oklch(0.74 0.11 195), var(--gradient-violet) = oklch(0.66 0.19 322) -->
</interfaces>

Locked decision D-02: teal→violet gradient carries functional meaning (criticality). The warning midpoint hue is `oklch(0.70 0.14 258)`. Accessibility (`aria-label`/`title` per dot) is already present and must be preserved — it mitigates the color-convention change from the old red/amber/green traffic-light mapping.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recolor criticality picker onto the teal→violet gradient (FOUND-03)</name>
  <files>src/components/tasks/criticality-picker.tsx</files>
  <read_first>
    - src/components/tasks/criticality-picker.tsx (full current state — hardcoded hex map + inline-style dots)
    - src/app/tasks/projects/page.tsx (lines around 13 and 127 — the sole other consumer of CRITICALITY_COLORS; confirm it reads the map as a plain string and needs NO edit)
    - .planning/phases/01-design-foundation-motion-system/01-PATTERNS.md (criticality-picker section — the render pattern to preserve verbatim)
  </read_first>
  <action>
    Replace the hardcoded hex values in `CRITICALITY_COLORS` with gradient-derived color strings: `on_track: "var(--gradient-teal)"`, `warning: "oklch(0.70 0.14 258)"` (the gradient midpoint), `critical: "var(--gradient-violet)"`. Critical keeps the highest chroma (0.19) so it still reads as the most urgent, compensating for the loss of the red/amber/green convention (per D-02 and RESEARCH Pitfall on urgency signal).

    The current dots build their fill/glow by string-concatenating hex + alpha (`${opt.hex}33`) and raw rgba glow values. That concatenation is INVALID once the base color is a `var()` or `oklch()` string (`"var(--gradient-teal)33"` is not a color). Restructure the `OPTIONS` shape and the inline `style` to derive translucent variants with `color-mix` instead: for each option carry a single base `color` string, then compute — selected: `backgroundColor: color`, `boxShadow: 0 0 7px 1px <glow>` where `glow = color-mix(in oklch, <color> 50%, transparent)`; unselected: `backgroundColor: color-mix(in oklch, <color> 20%, transparent)`, `border: 2px solid <color>`.

    Preserve EXACTLY, do not change: the `value`/`onChange` prop signature, `role="radiogroup"`/`role="radio"`/`aria-checked`/`aria-label`/`title` markup, the option order (critical, warning, on_track), the dot pixel sizes (selected 17×17, unselected 15×15), and the button's `hover:scale-110` interaction. Keep the `CRITICALITY_COLORS` export name and its three keys so `projects/page.tsx:127` (`borderColor: CRITICALITY_COLORS[project.criticality]`) keeps working with no edit there — a `var()`/`oklch()` string is a valid `borderColor`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5; echo "HEX_GONE=$(grep -Ec '#ef4444|#fbbf24|#10b981' src/components/tasks/criticality-picker.tsx)"; echo "GRADIENT_USED=$(grep -c -- '--gradient-violet' src/components/tasks/criticality-picker.tsx)"; echo "COLORMIX=$(grep -c 'color-mix' src/components/tasks/criticality-picker.tsx)"; echo "ARIA_PRESERVED=$(grep -c 'aria-checked' src/components/tasks/criticality-picker.tsx)"</automated>
    <human-check>Open the criticality picker (e.g. in the projects page add/edit flow); confirm 3 visually distinct dots along teal→violet, critical clearly most saturated. Confirm project left-borders on the projects page use the matching colors. Screenshot to gut-check the "which is more urgent?" read (RESEARCH Pitfall A1).</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -Ec '#ef4444|#fbbf24|#10b981' src/components/tasks/criticality-picker.tsx` returns `0` (no raw hex left)
    - File references `var(--gradient-teal)`, `var(--gradient-violet)`, and `oklch(0.70 0.14 258)`
    - File uses `color-mix(in oklch, ...)` for translucent fill/glow (no `${...}33` hex-alpha concatenation remains)
    - `aria-checked`, `aria-label`, and `title` still present on each dot
    - `CRITICALITY_COLORS` export retains keys `critical`, `warning`, `on_track`; `src/app/tasks/projects/page.tsx` is unmodified
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Criticality dots and project borders are gradient-driven and token-synced; accessibility markup and CRUD behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Token-driven hover/press feedback on nav + view tabs (MOTION-02)</name>
  <files>src/components/tasks/nav-tabs.tsx, src/components/tasks/view-tabs.tsx</files>
  <read_first>
    - src/components/tasks/nav-tabs.tsx (full — uses cn(); renders Link)
    - src/components/tasks/view-tabs.tsx (full — uses template-literal className; renders button)
    - src/lib/motion.ts (the exported constants to import)
    - .planning/phases/01-design-foundation-motion-system/01-PATTERNS.md (nav-tabs and view-tabs sections — per-file convention: keep cn() in nav-tabs, keep template literal in view-tabs; do NOT introduce cn() into view-tabs)
  </read_first>
  <action>
    In `nav-tabs.tsx`: import `transitionMicro` from `@/lib/motion`. In the `cn(...)` call for each Link, replace the bare `transition-colors` in the base string with `transitionMicro` (which is `transition-colors duration-micro ease-out`) so the tab hover is explicit-token-driven rather than relying on Tailwind's implicit 150ms default. Do NOT add press-translate here — this is a route `<Link>` and press-translate on navigation feels wrong (hover feedback is sufficient, per RESEARCH/PATTERNS). Keep the `-my-3`/`after:` active-underline markup and all NAV_ITEMS unchanged.

    In `view-tabs.tsx`: import `transitionMicro` and `pressFeedback` from `@/lib/motion`. In each tab button's template-literal className, replace bare `transition-colors` with `${transitionMicro} ${pressFeedback}` — this control is a plain `<button>`, so the `active:translate-y-px` press feedback is appropriate. Keep the template-literal style (do not refactor to cn()), the selected/unselected branch, sizes, and the `value`/`onChange`/`views` prop signature unchanged.

    This does not add the tab-switch CONTENT crossfade (MOTION-03) — that lives on the page content panels and is handled in Plan 01-03. This task is only the control-level hover/press feedback.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5; echo "NAV_IMPORT=$(grep -c 'from \"@/lib/motion\"' src/components/tasks/nav-tabs.tsx)"; echo "VIEW_IMPORT=$(grep -c 'from \"@/lib/motion\"' src/components/tasks/view-tabs.tsx)"; echo "VIEW_PRESS=$(grep -c 'pressFeedback' src/components/tasks/view-tabs.tsx)"; echo "NO_CN_IN_VIEWTABS=$(grep -c 'cn(' src/components/tasks/view-tabs.tsx)"</automated>
    <human-check>Hover and click-hold nav tabs and view tabs; confirm smooth ~150ms color transition and a subtle press-down on view tabs. Emulate `(hover: none)` in DevTools to confirm no sticky-hover regression.</human-check>
  </verify>
  <acceptance_criteria>
    - Both files import from `@/lib/motion`
    - `nav-tabs.tsx` uses `transitionMicro` and does NOT contain `active:translate-y-px`
    - `view-tabs.tsx` uses `transitionMicro` and `pressFeedback`; `grep -c 'cn(' src/components/tasks/view-tabs.tsx` returns `0` (convention preserved)
    - Prop signatures of both components unchanged (`value`/`onChange` for view-tabs; NAV_ITEMS/usePathname for nav-tabs)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Nav and view tabs have explicit, token-driven hover feedback; view tabs also have press feedback; motion.ts is consumed (not dead code).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Client-side restyle of existing components. No new input, data flow, auth, or network. CRUD callbacks unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Tampering | criticality-picker / tab components | accept | Pure presentation change; no new trust boundary (01-RESEARCH.md Security Domain: no ASVS L1 control applies). |
| T-01-SC | Tampering | package installs | N/A | No packages installed this phase. |
</threat_model>

<verification>
- `npm run build` and `npm run lint` exit 0.
- Grep assertions per task pass.
- Manual (01-VALIDATION.md): FOUND-03 visual dot check, MOTION-02 hover/press check including `(hover: none)` emulation.
</verification>

<success_criteria>
- Criticality picker + project borders use the teal→violet functional gradient with accessibility preserved.
- Nav/view tabs have explicit token-driven hover feedback; view tabs have press feedback.
- No prop/callback/accessibility changes (functional parity held).
</success_criteria>

<output>
Create `.planning/phases/01-design-foundation-motion-system/01-02-SUMMARY.md` when done.
</output>
