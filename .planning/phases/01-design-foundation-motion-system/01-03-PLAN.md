---
phase: 01-design-foundation-motion-system
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/tasks/layout.tsx
  - src/components/site-header.tsx
  - src/app/tasks/page.tsx
  - src/app/tasks/habits/page.tsx
  - src/app/tasks/statistics/page.tsx
  - src/app/tasks/projects/page.tsx
autonomous: true
requirements: [FOUND-04, FOUND-05, MOTION-03]
must_haves:
  truths:
    - "A static single-corner teal ambient glow is visible behind content on all four task-manager pages (Tasks, Habits, Projects, Statistics)"
    - "At ≥1024px the content container widens to max-w-app in lockstep across the header and all four pages"
    - "Switching day/week/month (Tasks) and the Statistics view range crossfades the content region (~200ms) instead of snapping"
  artifacts:
    - path: "src/app/tasks/layout.tsx"
      provides: "Shared layout rendering the ambient-glow element behind all /tasks pages"
      contains: "ambient-glow"
    - path: "src/components/site-header.tsx"
      provides: "Header container widened in lockstep with page content"
      contains: "lg:max-w-app"
  key_links:
    - from: "src/app/tasks/layout.tsx .ambient-glow div"
      to: "src/app/globals.css .ambient-glow rule"
      via: "className reference to the static radial-gradient rule"
      pattern: "className=\"ambient-glow\""
    - from: "view-switch content region"
      to: "tw-animate-css"
      via: "key={view} + animate-in fade-in duration-base"
      pattern: "key=\\{view\\}"
---

<objective>
Ship the shared structural + rollout pieces of the foundation: the ambient background glow across all four pages (FOUND-05), the desktop container-width lever applied in lockstep (FOUND-04 lever 2), and the view-switch content crossfade (MOTION-03).

Purpose: Complete the "instantly different" whole-app read — depth behind content, a fuller desktop layout, and smooth view transitions — while every page's content renders byte-identical (functional parity).
Output: A new `tasks/layout.tsx` plus mechanical width edits and two crossfade wrappers.
</objective>

<execution_context>
@/Users/erx/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/erx/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-design-foundation-motion-system/01-CONTEXT.md
@.planning/phases/01-design-foundation-motion-system/01-RESEARCH.md
@.planning/phases/01-design-foundation-motion-system/01-PATTERNS.md

Depends on Plan 01-01 tokens: `.ambient-glow` CSS rule, `max-w-app` utility (from `--container-app: 88rem`), and `duration-base` utility all exist in `globals.css`.

CLAUDE.md warning: this is Next.js 16 with breaking changes vs. training data. `tasks/layout.tsx` is routing-adjacent — before writing it, read the relevant App Router layout guide under `node_modules/next/dist/docs/` to confirm the current layout file convention. A layout only wraps `{children}`; it must not change any page's data or behavior.

RESEARCH Open Question #1 is resolved here: use a shared `src/app/tasks/layout.tsx` (DRY, applies to all four /tasks pages) rather than pasting the glow div into four pages.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared ambient-background layout for all /tasks pages (FOUND-05)</name>
  <files>src/app/tasks/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx (the existing root layout — the project's layout convention: default export, `{ children }: { children: React.ReactNode }`)
    - src/app/tasks/page.tsx (confirm pages under /tasks render their own SiteHeader + main; the layout must only ADD a sibling glow, not restructure)
    - node_modules/next/dist/docs/ (App Router layout guide — confirm Next.js 16 layout file signature before writing)
    - .planning/phases/01-design-foundation-motion-system/01-RESEARCH.md (Ambient Background section + Open Question #1)
  </read_first>
  <action>
    Create `src/app/tasks/layout.tsx` as a Server Component (no `"use client"`, no state) exporting a default `TasksLayout({ children }: { children: React.ReactNode })`. It returns a fragment containing `{children}` and a single decorative element `<div className="ambient-glow" aria-hidden />`. The `.ambient-glow` rule (created in Plan 01-01) is `position: fixed; z-index: -1; pointer-events: none`, so it paints one static teal radial glow behind content on every page nested under `/tasks` (Tasks, Habits, Projects, Statistics) — satisfying FOUND-05's "all four pages" with one file.

    Do not add any provider, data fetch, metadata override, or wrapper that changes layout flow — the glow is fixed-position and out of normal flow, so page content geometry is unchanged (functional parity). Match the existing kebab-adjacent file/convention and ESM named/default export style used by `src/app/layout.tsx`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5; echo "LAYOUT_EXISTS=$(test -f src/app/tasks/layout.tsx && echo 1 || echo 0)"; echo "AMBIENT_REF=$(grep -c 'ambient-glow' src/app/tasks/layout.tsx)"; echo "ARIA_HIDDEN=$(grep -c 'aria-hidden' src/app/tasks/layout.tsx)"; echo "NO_CLIENT=$(grep -c 'use client' src/app/tasks/layout.tsx)"</automated>
    <human-check>Visit all four pages (/tasks, /tasks/habits, /tasks/projects, /tasks/statistics); confirm a subtle teal glow in one corner behind content on each, with no readability loss. DevTools Performance idle recording shows no repaint activity (static paint).</human-check>
  </verify>
  <acceptance_criteria>
    - `src/app/tasks/layout.tsx` exists, is a default export taking `{ children }`, and contains no `"use client"`
    - It renders `<div className="ambient-glow" aria-hidden />` alongside `{children}`
    - `npm run build` exits 0 and all four /tasks routes still render
  </acceptance_criteria>
  <done>All four task-manager pages show the shared static ambient glow via one shared layout; page content behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Desktop container-width rollout in lockstep (FOUND-04 lever 2)</name>
  <files>src/components/site-header.tsx, src/app/tasks/page.tsx, src/app/tasks/habits/page.tsx, src/app/tasks/statistics/page.tsx, src/app/tasks/projects/page.tsx</files>
  <read_first>
    - src/components/site-header.tsx (line ~6 — the `max-w-5xl` container)
    - src/app/tasks/page.tsx (line ~58), src/app/tasks/habits/page.tsx (line ~33), src/app/tasks/statistics/page.tsx (line ~124), src/app/tasks/projects/page.tsx (line ~60) — each has the identical `max-w-5xl` main container
    - .planning/phases/01-design-foundation-motion-system/01-PATTERNS.md (container-width rollout — identical mechanical edit in 5 places)
  </read_first>
  <action>
    In all five files, change the container class `max-w-5xl` to `max-w-5xl lg:max-w-app` — the exact same mechanical edit in each location. This keeps the mobile width (`max-w-5xl` = 64rem) and widens to the new `max-w-app` (88rem, from Plan 01-01's `--container-app`) at ≥1024px. The header (`site-header.tsx`) MUST move in lockstep with the four page mains, or the header and content misalign at ≥1024px. Do NOT edit `src/app/page.tsx` (the root/non-tasks page is out of scope — only the four task-manager pages + shared header). Touch only the container className; change nothing else in these files.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5; echo "ROLLOUT_COUNT=$(grep -rc 'max-w-5xl lg:max-w-app' src/components/site-header.tsx src/app/tasks/page.tsx src/app/tasks/habits/page.tsx src/app/tasks/statistics/page.tsx src/app/tasks/projects/page.tsx | grep -c ':1')"; echo "ROOT_PAGE_UNTOUCHED=$(grep -c 'lg:max-w-app' src/app/page.tsx)"</automated>
    <human-check>At 1024/1280/1536px viewport widths, confirm header + content widen together and no fixed-rem chart/grid (e.g. Statistics KPI grid, Tasks week bar) overflows or clips (RESEARCH Pitfall: root font-size scaling + wider container interaction).</human-check>
  </verify>
  <acceptance_criteria>
    - Each of the 5 files contains exactly one `max-w-5xl lg:max-w-app`
    - `src/app/page.tsx` does NOT contain `lg:max-w-app` (out of scope, untouched)
    - No other class or logic changed in the 5 files
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Content + header widen in lockstep at ≥1024px, addressing the "empty screen" complaint via the second density lever.</done>
</task>

<task type="auto">
  <name>Task 3: View-switch content crossfade on Tasks + Statistics (MOTION-03)</name>
  <files>src/app/tasks/page.tsx, src/app/tasks/statistics/page.tsx</files>
  <read_first>
    - src/app/tasks/page.tsx (the `<main>` at ~58 wraps a `view === "week" ? (...) : (...)` ternary — the view-dependent content region)
    - src/app/tasks/statistics/page.tsx (the `<main>` at ~124 wraps a `space-y-6` block whose KPI grid + charts depend on `view`/range)
    - .planning/phases/01-design-foundation-motion-system/01-PATTERNS.md (view-tabs MOTION-03 note — the crossfade lives on the content panel, keyed by view)
    - node_modules/tw-animate-css/dist/tw-animate.css (confirm `animate-in`/`fade-in` compose with Tailwind `duration-*`)
  </read_first>
  <action>
    Add a keyed crossfade wrapper around each page's view-dependent content so React remounts it on view change and `tw-animate-css` fires a fade. Use `<div key={view} className="animate-in fade-in duration-base">` (200ms, from Plan 01-01's `--duration-base`).

    In `tasks/page.tsx`: wrap the entire `view === "week" ? (...) : (...)` ternary output (inside `<main>`) in the keyed div. The ternary's inner content stays byte-identical — only the wrapper is added.

    In `statistics/page.tsx`: wrap the view-dependent content region (the KPI grid + charts block inside `<main>`) in the keyed div. Keep all KPI/chart JSX identical; only add the wrapper. If the `ViewTabs` control sits inside the wrapped region, that is acceptable (it is controlled and re-mounts harmlessly), but prefer wrapping the data region so the control itself does not flash.

    The global `prefers-reduced-motion` rule from Plan 01-01 automatically neutralizes this animation when reduced motion is on — no per-component guard needed. Do not alter any data, `useState`, or handler logic.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5; echo "TASKS_KEYED=$(grep -c 'key={view}' src/app/tasks/page.tsx)"; echo "STATS_KEYED=$(grep -c 'key={view}' src/app/tasks/statistics/page.tsx)"; echo "FADE_TASKS=$(grep -c 'animate-in fade-in duration-base' src/app/tasks/page.tsx)"; echo "FADE_STATS=$(grep -c 'animate-in fade-in duration-base' src/app/tasks/statistics/page.tsx)"</automated>
    <human-check>Switch Tasks week↔month and Statistics view ranges; confirm a ~200ms crossfade (no instant snap). Enable reduced-motion emulation and confirm the crossfade collapses to instant. Confirm all data shown and CRUD actions are unchanged.</human-check>
  </verify>
  <acceptance_criteria>
    - `tasks/page.tsx` and `statistics/page.tsx` each contain a `<div key={view} className="animate-in fade-in duration-base">` wrapping the view-dependent content
    - No data/handler/`useState` logic changed in either file
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Day/week/month and Statistics view switches crossfade smoothly and respect reduced-motion; data and CRUD unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | A new server-component layout (static decorative element), className edits, and content wrappers. No new input, data flow, auth, or network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | tasks/layout.tsx + page wrappers | accept | Presentation-only; no trust boundary crossed (01-RESEARCH.md Security Domain: no ASVS L1 control applies). |
| T-01-SC | Tampering | package installs | N/A | No packages installed this phase. |
</threat_model>

<verification>
- `npm run build` and `npm run lint` exit 0.
- Grep assertions per task pass.
- Manual (01-VALIDATION.md): FOUND-05 glow on all 4 pages + idle-repaint check, FOUND-04 responsive overflow check at 1024/1280/1536px, MOTION-03 crossfade timing + reduced-motion collapse.
</verification>

<success_criteria>
- Static ambient glow visible behind all four task-manager pages via a shared layout.
- Container widens to `max-w-app` in lockstep across header + four pages at ≥1024px, no overflow.
- Tasks and Statistics view switches crossfade ~200ms and respect reduced-motion, with full data/CRUD parity.
</success_criteria>

<output>
Create `.planning/phases/01-design-foundation-motion-system/01-03-SUMMARY.md` when done.
</output>
